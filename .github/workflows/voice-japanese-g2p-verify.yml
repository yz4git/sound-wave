name: Voice Japanese G2P Verify

on:
  push:
    branches: [chatgpt/voice-lab-japanese-g2p]
    paths:
      - src/voice/**
      - tests/voice-japanese-g2p.test.ts
      - scripts/validate-production.mjs
      - .github/workflows/voice-japanese-g2p-verify.yml

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-audit --no-fund
      - run: npx vitest run tests/voice-script.test.ts tests/voice-expression.test.ts tests/voice-markup.test.ts tests/voice-synth-plan.test.ts tests/voice-speech-source.test.ts tests/japanese-phoneme.test.ts tests/voice-japanese-g2p.test.ts
      - run: npm run build
      - run: npm run validate:prod
